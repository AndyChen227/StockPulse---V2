# Application source / 应用源代码

**English:** This is a standard Python `src` layout. The installable `stockpulse` package lives in `src/stockpulse/`; keeping code here prevents accidental imports from the repository root. See [`stockpulse/README.md`](stockpulse/README.md) for the module map.

**中文：** 这里使用标准 Python `src` 布局。可安装的 `stockpulse` 包位于 `src/stockpulse/`，这样可以避免从仓库根目录意外导入未安装代码。模块地图见 [`stockpulse/README.md`](stockpulse/README.md)。
